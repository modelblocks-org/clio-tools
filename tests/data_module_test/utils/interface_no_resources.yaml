modelblocks_convention: 1.0.0
pathvars:
  snakemake_defaults:
    logs:
      default: "<logs>"
      description: location of rule log files.
    resources:
      default: "<resources>"
      description: "location of module resource files."
    results:
      default: "<results>"
      description: "location of module results."
  results:
    stuff:
      default: "<results>/stuff.parquet"
      description: Module results.
